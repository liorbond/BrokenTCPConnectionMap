#ifndef __BROKEN_TCP_CONNECTION_MAP__
#define __BROKEN_TCP_CONNECTION_MAP__ // def guard

/** 
 * Main function that implement the logic of mapping broken connections
 * Params:
 *  [pcap_path] - null terminated string indicates the path of the pcap file
 * Return:
 *  None
*/
void map_broken_tcp_connections(char* pcap_path);



#endif // __BROKEN_TCP_CONNECTION_MAP__